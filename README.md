# Tony / 唐胜斌

Static personal website adapted from the supplied Valtero HTML template. English is the primary language, with brief Chinese supporting copy.

## Preview locally

From this directory, run `python3 -m http.server 8000`, then open `http://localhost:8000`.

## Publishing

Live site: https://tony-fitness-journal.mq8zhfg9d2.chatgpt.site

The repository also includes a GitHub Pages workflow. To publish through GitHub Pages later, enable **Settings → Pages → Source: GitHub Actions**. Until that setting is enabled, the Pages workflow will fail and `https://tony132321.github.io/ve112/` will not be live.

## Content still pending

The original template's contact form, booking link, social links, and stock showreel remain until Tony supplies verified destinations and a personal video. The form has no working backend. The original template also has a nonfatal console error in its modal script when `.modal` is absent; its original JavaScript was left unchanged.

The site's photos are optimized copies without EXIF metadata. Original photos and the template ZIP stay outside this repository.
